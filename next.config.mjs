/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "demo-alukas.myshopify.com",
      "http://localhost:3001/",
      "cdn.shopify.com",
      "localhost",
    ],
  },
};

export default nextConfig;
